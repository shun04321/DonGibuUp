<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.spring.goods.dao.PurchaseMapper">
	<insert id="insertPurchase" parameterType="kr.spring.goods.vo.PurchaseVO">
    INSERT INTO purchase
    (purchase_num, mem_num, imp_uid, pay_price, pay_date, pay_status)
    VALUES
    (purchase_seq.nextval, #{memNum}, #{imp_uid}, #{amount}, SYSDATE, #{payStatus})
</insert>
	
	<insert id="insertRefund" parameterType="kr.spring.goods.vo.RefundVO">
    INSERT INTO refund
    (refund_num, mem_num, imp_uid, reason, refund_date)
    VALUES
    (refund_seq.nextval, #{memNum}, #{impUid}, #{reason}, SYSDATE)
	</insert>
	
	<select id="getPurchaseListByMember" parameterType="long" resultType="kr.spring.goods.vo.PurchaseVO">
        SELECT *
        FROM purchase
        WHERE mem_num = #{memNum}
    </select>
	
</mapper>