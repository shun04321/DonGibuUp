<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper   
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="kr.spring.category.dao.CategoryMapper">	
	<insert id="insertDonationCategory" parameterType="donationCategoryVO">
		INSERT INTO dona_category
		(dcate_num,
		 dcate_name,
		 dcate_charity,
		 dcate_icon,
		 dcate_content)
			 VALUES(
			 dona_category_seq.nextval,
			 #{dcate_name},
			 #{dcate_charity},
			 #{dcate_icon,jdbcType=VARCHAR},
			 #{dcate_content}
		)
	</insert>	
	<select id="selectList" parameterType="map" resultType="donationCategoryVO">
		 	SELECT
	 	   *
	    FROM (SELECT
	            a.*,
	            rownum rnum
	          FROM (SELECT
	                  dcate_num,
	                  <![CDATA[
	                  	REPLACE(REPLACE(dcate_name,'<','&lt;'),'>','&gt;') dcate_name,
	                  ]]>
	                  dcate_charity,
	                  dcate_icon,
	                  dcate_content
	                FROM dona_category 				<!-- 알리아스 -->
	                )a)
	 	<![CDATA[
	 	WHERE rnum >= #{start} AND rnum <= #{end}
	 	]]>
	</select>
</mapper>







